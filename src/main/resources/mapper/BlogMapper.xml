<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.Mapper.BlogMapper">
<select id="findAllblog" resultType="blog">
    select title,time date from article ORDER BY date DESC
</select>
    <select id="findblogByClass" resultType="blog">
        select title,time date from article inner join category on article.cid=category.cid where category.name=#{className}
        ORDER BY  date desc
    </select>

</mapper>
